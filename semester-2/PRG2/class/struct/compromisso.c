#include <stdio.h>

#define MAX_COMPROMISSOS 20;
typedef struct{
    char descricao[1024];
    char local[1024];
    int hora, minuto, dia, mes,ano;
}Compromissos_t ;

void adicionar_compromissos(){
    if (expression) {
    
    }
}
int main(){ 
    int opção;
    while (1) {
        printf("O que você deseja:\n");
        printf("1 - Adicionar um compromisso\n");
        scanf("%d", &opção);
        printf("2 - Lista um compromisso\n");
        scanf("%d", &opção);
        printf("Sair\n");
        scanf("%d", &opção);

        switch (opção) {
            case 1:
            break;;
            case 2:
            break;;
            case 3:
            break;
        }
    }



    return 0;
}